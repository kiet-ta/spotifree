<UserControl x:Class="Spotifree.Views.AlbumView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             Width="180" Height="240">
    <Grid>
        <materialDesign:Card UniformCornerRadius="5"
                             materialDesign:ElevationAssist.Elevation="Dp4"
                             Background="{DynamicResource MaterialDesignCardBackground}">
            <materialDesign:Card.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Rename Album"
                              Command="{Binding RenameCommand}"
                              CommandParameter="{Binding}">
                        <MenuItem.Icon>
                            <materialDesign:PackIcon Kind="RenameBox" />
                        </MenuItem.Icon>
                    </MenuItem>
                </ContextMenu>
            </materialDesign:Card.ContextMenu>

            <StackPanel>
                <Image Height="160" Stretch="UniformToFill">
                    <Image.Source>
                        <Binding Path="CoverArt">
                            <Binding.TargetNullValue>
                                <BitmapImage UriSource="pack://application:,,,/Assets/defaultImage.png" />
                            </Binding.TargetNullValue>
                        </Binding>
                    </Image.Source>
                </Image>

                <TextBlock Text="{Binding Name}"
                           FontWeight="Bold"
                           Margin="5,5,5,0"
                           TextTrimming="CharacterEllipsis"
                           Foreground="{DynamicResource MaterialDesignBody}"
                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}"/>

                <TextBlock Text="{Binding Artist}"
                           Margin="5,0,5,5"
                           TextTrimming="CharacterEllipsis"
                           Foreground="{DynamicResource MaterialDesignBody}"
                           Style="{StaticResource MaterialDesignBody2TextBlock}"/>

                <Button Content="View Album"
                        Command="{Binding DataContext.SelectAlbumCommand,
                                          RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                        CommandParameter="{Binding}"
                        Style="{StaticResource MaterialDesignRaisedButton}"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
