<UserControl x:Class="Spotifree.Views.TourView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Spotifree.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:TourViewModel, IsDesignTimeCreatable=False}"
             Visibility="{Binding IsTourVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
    <Grid Background="#80000000">

        <Border BorderBrush="White" BorderThickness="3"
                CornerRadius="5"
                Background="Transparent"
                
                Visibility="{Binding CurrentStep.ShowHighlight, Converter={StaticResource BooleanToVisibilityConverter}}"
                HorizontalAlignment="{Binding CurrentStep.HighlightAlignmentH}"
                VerticalAlignment="{Binding CurrentStep.HighlightAlignmentV}"
                Margin="{Binding CurrentStep.HighlightMargin}"
                Width="{Binding CurrentStep.HighlightWidth}"
                Height="{Binding CurrentStep.HighlightHeight}" />

        <Border HorizontalAlignment="{Binding CurrentStep.PopupAlignmentH}"
                VerticalAlignment="{Binding CurrentStep.PopupAlignmentV}"
                Margin="{Binding CurrentStep.PopupMargin}"
                
                Background="{DynamicResource MaterialDesignPaper}" CornerRadius="8"
                MaxWidth="300"
                Padding="16">

            <StackPanel>
                <TextBlock Text="{Binding CurrentStep.Text}"
                           TextWrapping="Wrap"
                           Style="{StaticResource MaterialDesignBody1TextBlock}"
                           Margin="0,0,0,16"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Content="Bỏ qua"
                            Command="{Binding EndTourCommand}" />

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                Content="Trước"
                                Command="{Binding PrevCommand}"
                                Margin="0,0,8,0" />

                        <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                Content="Sau"
                                Command="{Binding NextCommand}" />
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>