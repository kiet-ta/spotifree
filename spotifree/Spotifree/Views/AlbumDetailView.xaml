<UserControl x:Class="Spotifree.Views.AlbumDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#eee" Margin="10">
            <Button Content="← Back" Command="{Binding GoBackCommand}" Margin="5" />
            <Image Source="{Binding Album.CoverArt, FallbackValue='default_cover.png'}" Width="150" Height="150" />
            <StackPanel VerticalAlignment="Center" Margin="10">
                <TextBlock Text="{Binding Album.Name}" FontSize="24" FontWeight="Bold" />
                <TextBlock Text="{Binding Album.Artist}" FontSize="18" Foreground="Gray" />
                <Button Content="Play All" Command="{Binding PlayAlbumCommand}" Margin="0,10,0,0" />
            </StackPanel>
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding Album.Tracks}"
                  SelectedItem="{Binding SelectedTrack, Mode=OneWayToSource}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}" Width="300" />
                    <GridViewColumn Header="Artist" DisplayMemberBinding="{Binding Artist}" Width="200" />
                    <GridViewColumn Header="Duration (s)" DisplayMemberBinding="{Binding Duration}" Width="100" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>