<UserControl x:Class="Spotifree.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Spotifree.ViewModels"
             xmlns:models="clr-namespace:Spotifree.Models"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance vm:ChatViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="300">

    <Grid Background="#282828">
        <Button Style="{StaticResource MaterialDesignIconButton}"
                Command="{Binding HideCommand}"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Panel.ZIndex="99" 
                Margin="0,5,5,0"
                Foreground="Gray">
            <materialDesign:PackIcon Kind="Close" />
        </Button>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="5,35,5,5">
                <ItemsControl ItemsSource="{Binding Messages}" Foreground="White">
                </ItemsControl>
            </ScrollViewer>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>