<UserControl x:Class="Spotifree.Views.AlbumView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             Width="180" Height="240">
    <Grid>

        <materialDesign:Card UniformCornerRadius="5" materialDesign:ElevationAssist.Elevation="Dp4">
            <StackPanel>
                <Image Source="{Binding CoverArt, FallbackValue='default_cover.png'}" Height="160" Stretch="UniformToFill" />

                <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="5,5,5,0" TextTrimming="CharacterEllipsis" 
               Style="{StaticResource MaterialDesignSubtitle1TextBlock}"/>

                <TextBlock Text="{Binding Artist}" Margin="5,0,5,5" TextTrimming="CharacterEllipsis" 
                           Style="{StaticResource MaterialDesignBody2TextBlock}"/>

                <Button Content="View Album"
                        Command="{Binding DataContext.SelectAlbumCommand, 
                       RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                        CommandParameter="{Binding}" 
                        Style="{StaticResource MaterialDesignFlatButton}"/>
            </StackPanel>

        </materialDesign:Card>
    </Grid>
</UserControl>